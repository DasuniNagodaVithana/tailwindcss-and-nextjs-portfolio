/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/tailwindcss-and-nextjs-portfolio",
  output: "export",
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
